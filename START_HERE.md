# START_HERE · 给 AI Agent 的接入入口

这个文件是给 **AI Agent** 读的。人的入口是 [README.md](README.md)。

当前状态：**接入流程整体在建设中**。请先读下面的"现在能做/不能做"，再决定怎么回答用户。

适用 Agent：**WorkBuddy、Codex、ChatGPT、豆包办公、千问办公** 等一切能读文件、能调接口、能按步骤执行的 AI 助手。只要具备这三项能力，本流程通用。

---

## 第一步：判断用户意图

| 用户意图 | 你该怎么做 |
|---|---|
| **接入**领猫 SCM（"怎么连上我的系统"） | 走下面的接入流程；`connect/` 下的细则发布一条用一条 |
| **学技巧**（"这类事一般怎么做"） | 读 `playbooks/`（按经营问题组织）与 `knowledge/`（口径与术语） |
| **解决具体经营问题**（"本周哪些订单有延期风险"） | 读对应 `playbooks/` 场景 + 其引用的 `knowledge/` 条目，再执行 |

## 第二步：接入流程（六步）

1. **确认目标**：用户要接入哪个环境、要解决什么经营问题、要读哪些数据。
2. **检查环境**：是否具备联网、文件读取、执行工具的能力；记录 Agent 名称与版本。
3. **核对兼容性**：读 `connect/` 下的版本与前置条件说明。**没写的就是没确认，不要推测。**
4. **引导授权**：明确展示"要访问哪个企业、哪些数据范围、什么操作权限"，由**用户在官方渠道**完成授权。不要向用户索取密钥明文以外的任何凭证，也不要把凭证写进仓库。
5. **最小只读验证**：跑一次最小只读查询，把结果分为三类——**无数据 / 无权限 / 连接失败**，三者含义完全不同，不要混为一谈。
6. **出接入报告**：告知用户"连的是哪个环境、验证了什么、还有哪些限制"，再按需学习相关场景。

## 硬性边界

- **不推测**接口地址、字段含义、授权方式、已支持的功能。信息缺失就明确说"当前未发布"。
- **不越界**：只读接入不自动升级为写入；不做用户没要求的批量导出。
- **不泄露**：不索取、不公开密钥、原始对话、客户单据、价格利润、供应商与客户身份。
- **不代承诺**：不做报价、交付周期、人力投入的承诺；这些由领猫官方渠道给出。
- **不留痕**：日志与报错默认脱敏；不要把真实经营数据带进公开 Issue 或 PR。

## 现在能做 / 不能做

**可以做**：介绍项目定位与建设范围；帮用户梳理他要解决的经营问题；按已发布的 `knowledge/` 与 `playbooks/` 内容给方法；帮用户整理脱敏后的问题或贡献草稿。

**暂时不能做**：直接调用领猫 SCM 接口（`connect/api.md` 未发布）；安装领猫使用助手技能（规划中）；代为提交 Issue（规划中）。

## 用户可以直接复制的一段话

> 读一下 <https://github.com/IfLinkIf/linkmore-scm-ai-agent-playbook/blob/main/START_HERE.md>，
> 我想用 AI Agent 把领猫 SCM 里的经营数据用起来，先帮我判断该从哪一步开始。

---

维护：本项目由领猫科技维护，内容审核由项目负责人把关。贡献方式见 [CONTRIBUTING.md](CONTRIBUTING.md)。
